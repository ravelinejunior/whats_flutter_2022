import 'package:flutter/material.dart';

class ContactsTabScreen extends StatelessWidget {
  const ContactsTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Contacts'),
    );
  }
}
