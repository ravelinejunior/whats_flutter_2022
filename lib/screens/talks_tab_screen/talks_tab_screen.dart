import 'package:flutter/material.dart';

class TalksTabScreen extends StatelessWidget {
  const TalksTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Talks'),
    );
  }
}
